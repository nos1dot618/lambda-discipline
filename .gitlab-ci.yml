stages:
  - test

linux:
  stage: test
  image: python:3.12

  before_script:
    - apt-get update
    - apt-get install -y build-essential cmake ninja-build

  script:
    - python make.py --toolchain gcc setup
    - python make.py --toolchain gcc build
    - python make.py --toolchain gcc test
